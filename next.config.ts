import type { NextConfig } from "next";

/**
 * Next.js configuration for E-Waste Management System
 * Customize build settings, environment variables, and other options
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
